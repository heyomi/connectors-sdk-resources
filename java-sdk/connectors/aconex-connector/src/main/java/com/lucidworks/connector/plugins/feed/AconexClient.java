package com.lucidworks.connector.plugins.feed;

import com.lucidworks.connector.plugins.feed.model.ProjectList;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.net.URI;

public class AconexClient {
    private static final Logger logger = LoggerFactory.getLogger(AconexClient.class);

    public ProjectList getProjects(URI uri) {
        return null;
    }
}
